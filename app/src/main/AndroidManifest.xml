<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lxf.androiddemos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ViewDragHelperActivity" />
        <activity android:name=".ui.activity.BehaviorActivity" />
        <activity android:name=".ui.activity.ZxingActivity" />
        <activity android:name=".ui.activity.DatabindingActivity" />
        <activity android:name=".ui.activity.NetActivity" />
        <activity android:name=".ui.activity.IOCActivity" />
        <activity android:name=".ui.activity.FlowActivity" />
        <activity android:name=".ui.activity.ViewActivity" />

        <service android:name=".ui.service.MyIntentService" />

        <activity android:name=".ui.activity.UpdateActivity" />
        <activity android:name=".ui.activity.LottieActivity" />
        <activity
            android:name=".ui.activity.BannerActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".ui.activity.TransitionActivity"
            android:theme="@style/transitionTheme" />
        <activity
            android:name=".ui.activity.TransitionTargetActivity"
            android:theme="@style/transitionTheme" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.LoginTargetActivity"></activity>
    </application>

</manifest>